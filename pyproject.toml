[project]
name = "netwatch"
version = "1.0.0"
description = "Linux Network Monitoring Tool"
requires-python = ">=3.10"

dependencies = [
    "rich",
    "ping3",
    "psutil",
    "manuf"
]

[project.scripts]
netwatch = "netwatch.cli:main"
